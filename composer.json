{
    "name": "wildbit/postmark-client",
    "license" : "MIT",
    "description": "The officially supported client for Postmark (http://postmarkapp.com)",
    "require": {
        "php": ">=5.3.0",
        "guzzle/guzzle": "3.9.2"
    },
    "require-dev": {
    	"phpunit/phpunit": "4.4.0",
        "sphpdox/sphpdox": "dev-master"
    },
    "autoload": {
        "psr-0": {
            "Postmark\\" : "src/"
        }
    },
    "autoload-dev": {
        "classmap": [
            "src/",
            "tests/"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "document" : "mkdir -p docs && rm -r docs && mkdir -p docs && ./vendor/bin/sphpdox process --output=\"./docs\" Postmark src/Postmark"
    }
}